{
  "name": "untitled-folder",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "dev": "ts-node src/index.ts",
    "dev:4000": "PORT=4000 ts-node src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "start:pm2": "pm2 start dist/index.js --name 'api-server'",
    "stop:pm2": "pm2 stop api-server",
    "restart:pm2": "pm2 restart api-server",
    "test": "jest",
    "test:integration": "jest --testPathPattern=integration",
    "test:integration:watch": "jest --watch --testPathPattern=integration",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "type-check": "tsc --noEmit",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "clean": "npx kill-port 3000",
    "clean:all": "npx kill-port 3000 3001 5173 8080 8000",
    "predev": "npx kill-port 3000 || true",
    "setup:colima": "sh scripts/setup-colima.sh",
    "setup:pm2": "sh scripts/setup-pm2.sh",
    "build:pack": "sh scripts/build-with-pack.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/cookie-parser": "^1.4.9",
    "@types/express": "^5.0.3",
    "@types/jest": "^30.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "eslint": "^9.35.0",
    "jest": "^30.1.3",
    "kill-port": "^2.0.1",
    "prettier": "^3.6.2",
    "prisma": "^6.15.0",
    "ts-jest": "^29.4.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@prisma/client": "^6.15.0",
    "bcrypt": "^6.0.0",
    "cookie-parser": "^1.4.7",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "stripe": "^18.5.0"
  }
}